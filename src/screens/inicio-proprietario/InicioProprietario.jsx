import { SafeAreaView } from "react-native-safe-area-context";

import { Text } from "../../components";

const InicioProprietario = ({ navigation }) => {
	return (
		<SafeAreaView>
			<Text>Inicio Proprietario</Text>
			<Text>Inicio Proprietario</Text>
		</SafeAreaView>
	);
};

export default InicioProprietario;
